- **Qt 6 (C++)**：作为基础框架，利用 `QProcess` 进行进程间通信，`QSqlDatabase` 处理本地存储。
- **Windows DPAPI**：通过 `CryptProtectData` 实现 API Key 的用户级加密存储（见 `utils/crypto.h`）。
- **Python 互操作**：通过标准输入/输出流与外部 Python Agent 脚本进行基于 JSON 帧的结构化通信。