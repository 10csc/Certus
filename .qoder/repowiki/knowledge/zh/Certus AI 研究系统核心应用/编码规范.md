- 采用 PIMPL 风格的头文件组织，所有类定义均使用 `#pragma once` 防止重复包含。
- 核心业务逻辑通过 Qt 信号槽机制解耦，例如 `AgentManager` 发射状态信号供 UI 或 CLI 监听。
- 统一使用 `QString` 和 `QDir` 处理路径与字符串，确保跨平台兼容性及内部编码一致性。
- 数据库操作集中在 `Database` 类中，遵循“Python 不直接触碰 SQLite”的设计约束，所有写入经由 C++ 层中转。