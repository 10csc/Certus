- **浏览器自动化**：使用 `playwright` (CDP 模式) 连接外部 Edge/Chrome 浏览器实例。
- **通信协议**：自定义二进制长度前缀 JSON 帧（4-byte big-endian length + JSON payload）用于 stdin/stdout 流式通信。
- **持久化**：基于 JSON 文件的轻量级状态存储（`profiles/` 存放进化档案，`config.json` 存放运行时配置）。